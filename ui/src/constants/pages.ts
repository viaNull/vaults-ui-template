const VAULT_MANAGER_PATH = "/vault-manager";
const VAULTS_PATH = "/vaults";

export const PAGES = {
  vaultManagerHome: `${VAULT_MANAGER_PATH}/`,
  vaultManagerCreateVault: `${VAULT_MANAGER_PATH}/create-vault`,

  vaultsHome: `${VAULTS_PATH}/`,
};
